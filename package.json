{
  "name": "growsmm-vercel",
  "version": "1.0.0",
  "description": "GrowSMM — Vercel deployment (migrated from Netlify)",
  "private": true,
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "build": "echo \"Static site — no build step required\" && exit 0",
    "start": "vercel dev",
    "dev": "vercel dev"
  },
  "dependencies": {
    "firebase-admin": "^11.11.0",
    "imapflow": "^1.3.3",
    "mailparser": "^3.9.8",
    "node-fetch": "^2.7.0",
    "protobufjs": "^7.5.4"
  }
}
